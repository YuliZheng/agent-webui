# Copy this file to keystore.properties and keep it private.
# Reuse the same keystore for every future update or Android will reject it.
storeFile=.signing/agent-webui-release.jks
storePassword=replace-with-a-strong-password
keyAlias=agent-webui
keyPassword=replace-with-a-strong-password
